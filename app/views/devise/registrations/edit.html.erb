<!-- Page header -->
<div class="mb-8">

  <!-- Title -->
  <h1 class="text-2xl text-center md:text-3xl text-slate-800 font-bold"><%= t("titulo_cuenta") %> ✨</h1>

</div>

<div class="bg-white shadow-lg rounded-sm mb-8">
  <div class="flex flex-col md:flex-row md:-mr-px" data-controller="tabs" data-tabs-use-anchor="true" data-tabs-active-tab="bg-indigo-50">
    <div>

      <!-- Sidebar -->
      <%= render "sidebar" %>
    </div>

    <!-- Panel Account -->
    <%= render "account" %>

    <!-- Panel Fiscal Data -->
    <%= render "personal" %>

    <!-- Panel Address -->
    <%= render "address" %>

    <!-- Panel Preferencias -->
    <%= render "preferencias" %>

  </div>
</div>
