<div class="home mx-auto">
  <div class="bg-indigo-700 h-1"></div>
  <div class="container mx-auto">
    <header>
      <div class="flex items-center flex-shrink-0">
        <h1 class="title">Factura Zen</h1>
      </div>
      <div class="actions">
        <div class="relative">
          <% if current_user %>
            <%= render "action_button",
              caption: "Salir",
              icon: "ei-chevron-right",
              link: destroy_user_session_path,
              method: :delete %>
          <% else %>
            <%= render "action_button",
              caption: "Entrar",
              icon: "ei-chevron-right",
              link: new_user_session_path %>
          <% end %>
        </div>
      </div>
    </header>
  </div>

  <div class="relative">
    <div class="theatre container ">
      <div class="center">
        <h1 class="heading">
          Lo más fácil para facturar en España
        </h1>
        <p class="text-gray-700 text-xl md:pb-4">
          <% unless current_user %>
            <%= link_to "Empezar", new_user_registration_path %>
          <% end %>
            descubre las posibilidades
        </p>
      </div>
    </div>
  </div>
</div>