<% if notice && notice.present? %>
  <div class="bg-primary text-primary-foreground alert" role="alert">
    <%= notice %>
  </div>
<% end %>

<% if alert && alert.present? %>
  <div class="bg-destructive text-destructive-foreground alert" role="alert">
    <%= alert %>
  </div>
<% end %>
