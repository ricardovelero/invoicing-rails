<!DOCTYPE html>
<html lang=<%= I18n.locale %> class="h-full bg-background">
  <head>
    <%= render 'shared/head' %>
  </head>

  <body class="font-inter antialiased bg-background text-muted-foreground">
    <%= render "layouts/alert" %>
        <main>
            <%= yield %>
        </main>
  </body>
</html>
