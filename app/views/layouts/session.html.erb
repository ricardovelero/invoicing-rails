<!DOCTYPE html>
<html>
  <head>
    <%= render 'shared/head' %>
  </head>

  <body class="font-inter antialiased bg-slate-100 text-slate-600">
    <main class="container mx-auto mt-28 px-5 flex">
      <div class="m-auto w-84 md:w-96 session">
        <%= render "layouts/alert" %>
        
        <h1 class="title mb-6">
          <%= content_for(:page_title) %>
        </h1>
        <%= yield %>
      </div>
    </main>
  </body>
</html>
